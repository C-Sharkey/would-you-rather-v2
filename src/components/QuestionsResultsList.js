import React, { Component } from 'react'
import { connect } from 'react-redux'

class QuestionResultsList extends Component {
  render () {
    return (
        <div>
            Question Results List
        </div>
    )
  }
}

export default connect(QuestionResultsList)